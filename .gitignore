# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Editors / IDEs
.idea/
.vscode/
*.swp
*.swo
*~

# Node.js / pnpm
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.npm/
.yarn/
.pnpm-store/

# Build artefacts (CRX, XPI, ZIPs are produced by build scripts; do not commit
# from arbitrary locations — only the published copies under docs/ are tracked)
dist/
build/
*.zip
*.crx
*.xpi

# Allow the published copies under docs/ to be committed (served by GitHub Pages)
!docs/**/*.crx
!docs/**/*.xpi

# Signing keys — NEVER commit these. Treat like SSH private keys.
*.pem
keys/
.keys/

# Local AI tooling
.claude/

# Logs
*.log

# Environment files
.env
.env.local
.env.*.local
